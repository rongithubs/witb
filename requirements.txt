# To set up development environment:
# cd witb-backend
# source venv/bin/activate
# pip install -r ../requirements.txt
# pip install -r requirements-dev.txt
# uvicorn main:app --reload

# FastAPI backend
fastapi
uvicorn[standard]
greenlet
# Database & ORM
sqlalchemy>=2.0
asyncpg  # if you're using PostgreSQL (e.g., Supabase)
aiosqlite  # for async SQLite support

# HTTP requests & scraping
httpx
beautifulsoup4
lxml
scrapingbee

# Environment config
python-dotenv

# Optional: pydantic v2 for validation (FastAPI v0.100+ uses pydantic v2+)
pydantic>=2.0

# Optional: for data parsing
requests

# AI/ML for scraping
google-generativeai

# Scheduling
apscheduler>=3.10.0

# Authentication with Supabase
supabase>=2.10.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6
